# Minimal runtime deps for the local/Docker build.
# Excludes Emergent-platform-only packages (emergentintegrations, internal litellm wheel),
# which are only required for AI notes / concept images on the Emergent platform.
fastapi==0.110.1
uvicorn==0.25.0
motor==3.3.1
pymongo==4.6.3
pydantic==2.13.4
email-validator==2.3.0
bcrypt==4.1.3
PyJWT==2.13.0
requests==2.34.2
python-dotenv==1.2.2
python-multipart==0.0.32
pypdf==6.16.1
python-docx==1.2.0
